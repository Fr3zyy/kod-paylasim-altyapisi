<html>
  <body>
    <%- include('partials/_header'); -%>
    <div class="container mx-auto mt-16">
      <h1 class="text-center  text-white text-3xl font-bold">Hatalı/Yasak kod bildir</h1>
      <h4 class="text-center text-white">Hatalı/kurallara aykırı kodları bildirerek bize yardımcı olabilirsin.</h4>      <div>
        <br>
        <form action="/bug" method="POST" class="max-w-md mx-auto">
          <div class="mb-4">
            <label for="codeID" class="block mb-2 text-white  font-semibold">Kod ID:</label>
            <% if (codeID) { %>
            <input type="text" id="codeID" class="border text-white bg-gray-800 px-4 py-2 rounded-lg w-full" value="<%= codeID %>" name="id" placeholder="Kod ID" required>
            <% } else { %>
            <input type="text" id="codeID" class="border text-white bg-gray-800 px-4 py-2 rounded-lg w-full" name="id" placeholder="Kod ID" required>
            <% } %>
          </div>
          <div class="mb-4">
            <label for="bug" class="block mb-2 text-white font-semibold">Bulduğunuz hata:</label>
            <textarea class="form-textarea border text-white bg-gray-800 px-4 py-2 rounded-lg w-full" id="bug" style="resize: none;" name="bug" rows="10" placeholder="Hata bulduysanız bulduğunuz hata, yoksa yetkililerin görmesi için yorumunuz." required></textarea>
          </div>
          <button type="submit" class="bg-blue-500 hover:bg-blue-600 text-white font-bold py-2 px-4 rounded-lg w-full">Paylaş</button>
        </form>
      </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.5.0.js"></script>
    <script>
      if (localStorage.getItem("mode") != "") {
        document.body.classList.add(localStorage.getItem("mode"));
        document.getElementById("mode-btn").checked = true;
      }
    </script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta1/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>